# Papers

Current draft: `Trade Offs between TRM/LDT Hybrids`

```text
hybrid_ldt_trm_draft.md
```

This draft is based on the local benchmark outputs and excludes VPD from the hybrid benchmark argument. The
new-model review section incorporates RSITopology identity mathematics and clearly labels its saved cases as a
synthetic control-flow exercise rather than neural training performance.

The updated draft also includes the registered sequencer-control benchmark, paired statistical analysis, a
topology-control diagram, and the Verifiers `0.1.14` v1 replay package boundary.

The AIRIS/DAS extension adds a paired 6,670-trial forecast-integrity experiment. It distinguishes semantic
topology authorization from rule transport integrity and does not claim learned AIRIS or native DAS robustness.
