# Generalize Beyond Storyworld

Goal: add scaffolding for Metta code/mechanics frames while keeping storyworld as the first gym.

Tasks:

1. Add dataclasses for `ExecutionFrame`, `DeductionFrame`, `RepairFrame`, and `RoutingFrame`.
2. Add JSONL serialization helpers.
3. Add a CLI stub that reads a synthetic Metta-like rule file and emits frames.
4. Do not require a real MeTTa interpreter yet.

Deliverable: tests plus docs update.
