# Imported Fable Task Summary

The uploaded task frames the initial substrate as typed-soundness lattice reachability in coupled-objective storyworlds. Its core contribution is a typed conflict signal with epistemic provenance: environment-sound, model-sound, and experience-sound. It proposes literature clearance, formalization, experiment specs, and a safety/interp pitch skeleton.

This repo generalizes that task into a hybrid LDT/TRM gym. Storyworlds remain the first environment because they provide exact transitions and computable reachability labels, but the repo is structured so Metta code frames and Metta-defined deduction mechanics can be added as additional gyms.
