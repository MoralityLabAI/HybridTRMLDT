# Checkpoint: LSPG Architecture Protocol v1

## Completed

- Defined the bounded `K={2,3,4}`, `L={6,8}` schedule-word search.
- Separated 12 first-batch candidates from 12 one-time reserve candidates.
- Added matched periodic controls, balanced schedule nulls, and tied/untied context models.
- Added a primary-source literature registry and limited novelty to in-grammar non-equivalence.
- Added sealed theory, empirical, decision, resource, manifest, and receipt outputs.

## Tests

```text
18 focused architecture tests passed
proposal round trip and derived-artifact tamper rejection passed
```

## Decision

No architecture is named or promoted before registration. The proposal generator
must verify every registered input hash and must reproduce all 38 proposal hashes.

## Next Step

Commit the construction, register its exact input hashes, and generate the sealed
proposal corpus before resource calibration or task evaluation.
